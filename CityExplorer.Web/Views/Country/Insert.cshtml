@model CityExplorer.Web.InputModels.CountryInputModel
@{
    ViewBag.Title = "Insert Country";
}

<h2>@ViewBag.Title.</h2>

@using (Html.BeginForm("Insert", "Country", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    <hr />
    @Html.AntiForgeryToken()
    @Html.ValidationSummary("", new { @class = "text-danger" })
    <div class="form-group">
        <div class="row">
            @Html.LabelFor(m => m.Name, new { @class = "col-md-2 control-label text-right" })
            <div class="col-md-10">
                @Html.TextBoxFor(m => m.Name, new { @class = "form-control" })
            </div>
        </div>
    </div>
    <div class="form-group">
        <div class="row">
            <div class="col-md-2 text-right">
                <label for="Photo" class="control-label">Upload Image:</label>
            </div>
            <div class="col-md-10">
                <input type="file" class="form-control" name="Photo" id="Photo" style="width: 100%;" />
            </div>
        </div>
        <br />
        <input type="submit" value="Add" class="btn btn-primary btn-lg center-block" />
    </div>
}

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}